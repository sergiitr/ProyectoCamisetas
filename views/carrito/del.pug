block head
  title Confirmar Eliminación

block content
  .container
    h1.my-4 🗑️ Eliminar Producto del Carrito

    .alert.alert-danger(role='alert')
      h2 ¡Confirmación Requerida!
      h3 ¿Estás **totalmente seguro** de que quieres eliminar la camiseta **#{camiseta.marca} - #{camiseta.color} (#{camiseta.talla})** de tu carrito?

    .card.shadow-sm.mb-4
      .card-body
        h5 Detalles del Artículo
        ul.list-unstyled
          li **Cantidad en Carrito:** **#{cantidadActual}** unidades
          li **Precio de Venta (Registro):** $#{precioVenta}

    form(action=`/carro/del/camiseta/${camiseta.id}`, method='POST')
      button.btn.btn-danger.me-2(type='submit') Sí, Eliminar del Carrito
      a.btn.btn-secondary(href='/carro/list') No, Volver al Carrito
