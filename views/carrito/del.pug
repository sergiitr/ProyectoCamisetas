block head
  style.
    .confirm-container {
      max-width: 600px;
      margin: auto;
    }
    .alert-danger {
      background-color: #f8d7da;
      color: #842029;
      border-color: #f5c2c7;
    }
    .card {
      border-radius: 0.75rem;
    }
    .btn-danger {
      min-width: 140px;
    }
    .btn-secondary {
      min-width: 140px;
    }

block content
  .confirm-container.my-5
    h1.text-center.mb-4 Confirmar Eliminación

    .alert.alert-danger.text-center
      h4 ¡Confirmación requerida!
      p.fs-5 | ¿Deseas eliminar la camiseta
        strong #{camiseta.marca} - #{camiseta.color} (#{camiseta.talla})
        |  de tu carrito?

    .card.shadow-sm.mb-4
      .card-body
        ul.list-unstyled.fs-5
          li **Cantidad en carrito:** #{cantidadActual}
          li **Precio unitario:** #{precioVenta}€

    .d-flex.justify-content-center.gap-3
      form(action=`/carrito/del/camiseta/${camiseta.id}`, method='POST')
        button.btn.btn-danger.btn-lg(type='submit') Sí, eliminar
      a.btn.btn-secondary.btn-lg(href='/carrito') No, volver
